spring:
  application:
    name: summer-consumer
  main:
    #SpringBoot2.1需要设定
    allow-bean-definition-overriding: true

  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
      #Nacos注册中心地址
      discovery:
        server-addr: 127.0.0.1:8848